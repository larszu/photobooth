--- server-raspberry.js.orig	2025-01-20 10:00:00.000000000 +0100
+++ server-raspberry.js	2025-01-20 10:00:00.000000000 +0100
@@ -332,15 +332,17 @@
       try {
         // Test camera access
         await execAsync('gphoto2 --summary');
         console.log('✅ Camera access test successful');
+        this.isInitialized = true;
       } catch (error) {
         console.warn('⚠️ Camera access test failed:', error.message);
+        this.isInitialized = false;
+        return;
       }
     } catch (error) {
       console.error('❌ Camera initialization failed:', error);
+      this.isInitialized = false;
+      return;
     }
-    
-    this.isInitialized = true;
     console.log('✅ Camera initialized successfully');
   }
